# Dependencias de DESARROLLO/CI: runtime completo + test + tooling de análisis.
# requirements-dev.lock resuelve este archivo (runtime incluido) y puede
# arrancar la app para E2E; requirements.lock contiene sólo runtime.
# Los pins exactos viven en el lock generado en python:3.12 (Linux).
-r requirements.in

# Test
pytest>=8.0.0,<10
pytest-asyncio>=0.23.0,<2

# Lock/resolución reproducible
pip-tools>=7.4.0,<8

# Análisis estático y de seguridad (CI gates de Tarea 12)
ruff>=0.4.0,<1
mypy>=1.10.0,<2
pip-audit>=2.7.0,<3
detect-secrets>=1.5.0,<2
