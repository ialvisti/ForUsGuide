# Dependencias con rangos ACOTADOS (Task 12): lower bound = minimo probado,
# upper bound = siguiente major (evita que un deploy arrastre breaking
# changes). El lock exacto (requirements.lock) lo genera Cloud Build DENTRO
# de la imagen python:3.12 (ver cloudbuild.yaml) - no generarlo desde macOS
# ni otra version de Python.

# Core dependencies
pinecone>=9.1.0,<10
openai>=1.0.0,<3
google-genai>=1.0.0,<2
python-dotenv>=1.0.0,<2

# API Framework
fastapi>=0.110.0,<1
uvicorn[standard]>=0.27.0,<1
pydantic>=2.0.0,<3
pydantic-settings>=2.0.0,<3

# Data processing
tqdm>=4.66.0,<5

# Testing
pytest>=8.0.0,<10
pytest-asyncio>=0.23.0,<2
httpx>=0.27.0,<1

# Utilities
python-multipart>=0.0.9,<1
tiktoken>=0.5.0,<1
cachetools>=5.3.0,<7

# GCP Services
google-cloud-firestore>=2.16.0,<3
google-cloud-tasks>=2.16.0,<3
google-cloud-storage>=2.16.0,<4
google-cloud-logging>=3.10.0,<4
